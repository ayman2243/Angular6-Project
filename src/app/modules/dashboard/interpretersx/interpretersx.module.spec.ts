import { InterpretersxModule } from './interpretersx.module';

describe('InterpretersxModule', () => {
  let interpretersxModule: InterpretersxModule;

  beforeEach(() => {
    interpretersxModule = new InterpretersxModule();
  });

  it('should create an instance', () => {
    expect(interpretersxModule).toBeTruthy();
  });
});
