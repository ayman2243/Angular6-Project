import { SearchPermissionsPipe } from './search-permissions.pipe';

describe('SearchPermissionsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPermissionsPipe();
    expect(pipe).toBeTruthy();
  });
});
