import { SearchSurahPipe } from './search-surah.pipe';

describe('SearchSurahPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSurahPipe(null);
    expect(pipe).toBeTruthy();
  });
});
