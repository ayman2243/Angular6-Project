import { SearchAnnotationPipe } from './search-annotation.pipe';

describe('SearchAnnotationPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAnnotationPipe(null);
    expect(pipe).toBeTruthy();
  });
});
