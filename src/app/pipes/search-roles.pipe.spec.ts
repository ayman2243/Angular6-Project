import { SearchRolesPipe } from './search-roles.pipe';

describe('SearchRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
