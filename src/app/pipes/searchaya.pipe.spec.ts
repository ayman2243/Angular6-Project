import { SearchayaPipe } from './searchaya.pipe';

describe('SearchayaPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchayaPipe(null);
    expect(pipe).toBeTruthy();
  });
});
