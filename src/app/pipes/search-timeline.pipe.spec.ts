import { SearchTimelinePipe } from './search-timeline.pipe';

describe('SearchTimelinePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTimelinePipe(null);
    expect(pipe).toBeTruthy();
  });
});
