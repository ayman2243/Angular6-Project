import { SearchResourcePipe } from './search-resource.pipe';

describe('SearchResourcePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchResourcePipe(null);
    expect(pipe).toBeTruthy();
  });
});
