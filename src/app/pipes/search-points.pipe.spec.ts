import { SearchPointsPipe } from './search-points.pipe';

describe('SearchPointsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPointsPipe(null);
    expect(pipe).toBeTruthy();
  });
});
